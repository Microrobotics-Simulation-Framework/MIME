name: Publish to PyPI

# Publishes `mime-engine` to PyPI on every GitHub Release whose tag starts
# with `v` (e.g. v0.1.0). Uses PyPI Trusted Publishing (OIDC) — no API
# token is stored in the repo; the publisher is configured at
# https://pypi.org/manage/account/publishing/ with:
#   project       : mime-engine
#   owner         : Microrobotics-Simulation-Framework
#   repository    : MIME
#   workflow      : publish.yml
#   environment   : pypi
#
# A successful run can also be triggered manually via the Actions tab
# (workflow_dispatch).

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write   # required for Trusted Publishing OIDC

jobs:
  build:
    name: Build sdist + wheel
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build
        run: python -m pip install --upgrade pip build

      - name: Build distributions
        run: python -m build

      - name: Sanity-check distributions
        run: |
          python -m pip install --upgrade twine
          python -m twine check dist/*

      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
