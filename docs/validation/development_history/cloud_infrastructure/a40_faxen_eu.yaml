# Faxén force extraction experiment — EU region (avoid CA machine issues)

resources:
  cloud: runpod
  accelerators: A40:1
  use_spot: true
  image_id: ghcr.io/microrobotics-simulation-framework/mime:usd-gl

run: |
  echo "launched"

workdir: "."
